# Source notes

This project follows the supplied laboratory PDF for Variant 7:
- context: illustration for an article about quantum computing;
- abstraction rather than pseudo-documentary representation;
- required repeat and SHA-256 comparison;
- conceptuality must be explicit;
- do not present the image as a real-device diagram.

Current compatibility was checked on 23 Sep 2026:
- PyTorch publishes CPU wheels for CPython 3.14 on Windows x86-64.
- Diffusers 0.40.0 is published on PyPI with Python >=3.10 metadata.

These notes are only compatibility notes; the actual installed versions used in the experiment
must be recorded by `python -m pip freeze > reports/environment.txt`.
